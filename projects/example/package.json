{
  "name": "@blueprintui/example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "ci": "pnpm run clean && pnpm run build",
    "start": "drafter build --watch",
    "build": "drafter build",
    "clean": "rm -rf ./drafter"
  },
  "devDependencies": {
    "@blueprintui/drafter": "workspace:*"
  }
}