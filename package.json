{
  "name": "blueprintui-drafter-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "ci": "pnpm run build:example && pnpm run test",
    "test": "node --test tests/*.test.js",
    "setup": "pnpm run clean && pnpm install && pnpm run ci",
    "build:example": "cd projects/example && pnpm run build",
    "clean": "git clean -dfX"
  },
  "devDependencies": {
    "lite-server": "2.6.1"
  }
}